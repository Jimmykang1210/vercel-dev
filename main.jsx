import React from "react";
import ReactDOM from "react-dom/client";
import AEOOptimizer from "./aeo-optimizer.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <AEOOptimizer />
  </React.StrictMode>
);
